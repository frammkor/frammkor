export default class Job {
  constructor(title, companyName, startDate, endDate, descriptions) {
    this.title = title;
    this.companyName = companyName;
    this.startDate = startDate;
    this.endDate = endDate;
    this.descriptions = descriptions;
  }
}
